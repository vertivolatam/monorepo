{
  "name": "vertivo_mobile",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "flutter build apk",
    "dev": "flutter run",
    "test": "flutter test",
    "lint": "dart analyze",
    "format": "dart format lib/",
    "clean": "flutter clean"
  }
}
