name: vertivo_dashboard
description: Vertivo IoT Admin Dashboard — Jaspr SSR + D3.js

environment:
  sdk: ^3.8.0

dependencies:
  jaspr: ^0.22.1
  jaspr_router: ^0.6.0

dev_dependencies:
  build_runner: ^2.4.0
  build_web_compilers: ^4.4.6
  jaspr_builder: ^0.22.1
  jaspr_lints: ^0.3.0
  lints: ^5.0.0

jaspr:
  mode: server
  port: 8080
